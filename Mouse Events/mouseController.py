from pynput.mouse import Controller

def controlMouse():
    
    mouse = Controller()
    
    # ! Sets the position of the mouse  on (x,y) axis.
    
    # * mouse.position = (10,20)
    # * mouse.position = (100,200)
    # * mouse.position = (500,1000)
    
# * Function Call
controlMouse()